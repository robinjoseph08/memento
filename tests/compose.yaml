services:
  postgres:
    image: postgres:17.7-alpine3.23
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: test-admin-only-password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 1s
      timeout: 3s
      retries: 30
    tmpfs:
      - /var/lib/postgresql/data

  immich:
    image: caddy:2.10.2-alpine
    command:
      [
        "caddy",
        "run",
        "--config",
        "/etc/caddy/Caddyfile",
        "--adapter",
        "caddyfile",
      ]
    volumes:
      - ./fixtures/immich.Caddyfile:/etc/caddy/Caddyfile:ro

  memento:
    image: "memento:${MEMENTO_TEST_IMAGE_TAG:-test}"
    build:
      context: ..
    environment:
      LOG_FORMAT: json
      MEMENTO_DATABASE_URL: postgresql://memento_app:test-only-password@postgres:5432/memento?sslmode=disable
      MEMENTO_IMMICH_URL: http://immich:3001
      MEMENTO_IMMICH_API_KEY: test-only-key
      MEMENTO_SECURITY_SECRET: test-only-security-secret-32-bytes
      MEMENTO_SECURITY_INVITATION_ACCEPT_IP_LIMIT: 2
      MEMENTO_HTTP_PUBLIC_URL: http://localhost:8080
      MEMENTO_CADDY_TRUSTED_PROXY_CIDRS: private_ranges
      MEMENTO_HTTP_ADDRESS: 127.0.0.1:8091
      MEMENTO_HTTP_SHUTDOWN_TIMEOUT: 8s
    depends_on:
      postgres:
        condition: service_healthy
      immich:
        condition: service_started
    ports:
      - "127.0.0.1::8080"
    stop_grace_period: 10s

  front:
    image: caddy:2.10.2-alpine
    volumes:
      - ./fixtures/front.Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      memento:
        condition: service_started
    ports:
      - "127.0.0.1::8443"
